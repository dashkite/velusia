form

  header( part = "L1" )
    h1 Connect

  main
    form-field(
      name = "email"
      type = "email"
      required = true
      error = errors ? ( errors.email ? errors.email.message : undefined ) : undefined 
    )

  footer
    button Connect
