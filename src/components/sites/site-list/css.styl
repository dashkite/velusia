:host
  display flex
  flex-flow column
  gap 2rem
  min-inline-size 16rem

main
  display flex
  flex-flow column
  gap 1rem

footer
  border-top 1px solid var(--accent)
  padding 1rem
  & > nav
    flex-basis 100%
    justify-content end
